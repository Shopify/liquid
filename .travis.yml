sudo: false
cache: bundler
language: ruby

rvm:
  - 2.0
  - 2.1
  - 2.2
  - 2.3.0
  - ruby-head
  - jruby-head

addons:
  apt:
    packages:
    - libgmp3-dev

matrix:
  allow_failures:
    - rvm: jruby-head

script: "bundle exec rake"

notifications:
  disable: true
